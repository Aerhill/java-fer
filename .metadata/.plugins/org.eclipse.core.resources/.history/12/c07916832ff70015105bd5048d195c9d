package hr.fer.zemris.java.custom.scripting.elems;

/**
 * Base class to represent an element of language.
 * 
 * @author Ante Spajic
 *
 */
public class Element {

	// I'm writing this message because there is a
	// chance I will forget to write this tag and since some reviewers don't
	// know this and care so much about javadoc.
	// All classes inherit this javadoc automatically when class overrides this
	// method, no need for some tags like {inherit-javadoc} etc.
	/**
	 * Returns this elements value text representation.
	 * 
	 * @return This elements value as text.
	 */
	public String asText() {
		return "";
	}
}

package hr.fer.zemris.java.tecaj.hw1;

public class HofstadterQ {

	public static void main(String[] args) {

		int n = Integer.parseInt(args[0]);
		if( n > 0 ) {
			System.out.println("You requested calculation of "+ n + ". "
					+ "number of Hofstadter's Q-sequence. The requested number is " + Hofstadter(n) + ".");
		} else {
			System.out.println("Wrong arguments. The argument (number n) must be an postive integer.");
		}
	}

	private static long Hofstadter(int n) {
		long Q[] = new long[n+1];
		Q[1] = Q[2] = 1;
		for(int i = 3; i <= n; i++) {
			Q[i] = Q[(int) (i - Q[i - 1])] + Q[(int) (i - Q[i - 2])];
		}
		return Q[n];
	}

}
